FROM {{postgres.base}}:{{postgres.alpine.version}}

ENV POSTGRES_USER=${POSTGRES_USER}
ENV POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
ENV POSTGRES_DB=postgres

EXPOSE "5432"

CMD ["docker-entrypoint.sh", "postgres"]